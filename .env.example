# Copy this file to .env and fill in your key to enable real AI generation.
# Without a key, the server runs in DEMO MODE with realistic template-based
# output, so the prototype works out of the box.

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
PORT=3000
